// Take two numbers and present the larger one. If both are equal, only one of them should be presented.
const num1 = prompt("Insert first num");
const num2 = prompt("Insert second num");
document.write(`The bigger num is : ${(num1 > num2 ) ? num1 : num2}`);
