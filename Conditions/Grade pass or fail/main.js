// Enter a grade and display "pass" if the grade
// is greater than or equal to 60, otherwise display "failed".

const grade =+prompt("Insert your grade");
if(grade>=60)
{
    document.write("Pass");
}
else{
    document.write("Faile");
}